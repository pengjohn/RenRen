/*!
*@FileName: RenRenAPI_GuestbookGet.cpp
*@Author: pengzhixiong@GoZone
*@Date: 2010-12-30
*@Log: 	Author 			Date 		Description
*
*@section Copyright
* =======================================================================<br>
*          Copyright ? 2010-2012 GOZONE <br>
*                All Rights Reserved.<br>
* The file is generated by ReeRen_Component Wizard for Tranzda Mobile Platform <br>
* =======================================================================<br>
*/

#include "RenRenAPICommon.h"

/**
 * JsonÎÄ¼þ½âÎö
 *
 * \param text
 *
 * \return 
 */
void* RenRenAPI_JsonParse_GuestbookGet(char *text)
{
	int i = 0;
	cJSON *jsonall;
	cJSON *json;
	cJSON *pTemp0;
	tResponseGuestbookGet*  Response = new tResponseGuestbookGet;
	memset(Response, 0 , sizeof(tResponseGuestbookGet));

	jsonall = cJSON_Parse(text);

	Response->nArraySize = cJSON_GetArraySize(jsonall);
	if(Response->nArraySize>0)
	{
		Response->Array = (tResponseGuestbookGetItem*) malloc(sizeof( tResponseGuestbookGetItem) * Response->nArraySize );
		memset(Response->Array, 0, sizeof( tResponseGuestbookGetItem) * Response->nArraySize);
		for(i = 0; i<Response->nArraySize; i++)
		{
			json = cJSON_GetArrayItem(jsonall, i);

			pTemp0 = cJSON_GetObjectItem(json, "id");
			if(pTemp0)
			{
				Response->Array[i].id = pTemp0->valuedouble;
			}
			pTemp0 = cJSON_GetObjectItem(json, "uid");
			if(pTemp0)
			{
				Response->Array[i].uid = pTemp0->valueint;
			}
			pTemp0 = cJSON_GetObjectItem(json, "name");
			if(pTemp0)
			{
				STRCPY_Ex(Response->Array[i].name, pTemp0->valuestring);
			}
			pTemp0 = cJSON_GetObjectItem(json, "headurl");
			if(pTemp0)
			{
				STRCPY_Ex(Response->Array[i].headurl, pTemp0->valuestring);
			}
			pTemp0 = cJSON_GetObjectItem(json, "content");
			if(pTemp0)
			{
				STRCPY_Ex(Response->Array[i].content, pTemp0->valuestring);
			}
			pTemp0 = cJSON_GetObjectItem(json, "time");
			if(pTemp0)
			{
				STRCPY_Ex(Response->Array[i].time, pTemp0->valuestring);
			}
			pTemp0 = cJSON_GetObjectItem(json, "is_whisper");
			if(pTemp0)
			{
				Response->Array[i].is_whisper = pTemp0->valueint;
			}
			pTemp0 = cJSON_GetObjectItem(json, "source");
			if(pTemp0)
			{
				Response->Array[i].source = pTemp0->valueint;
			}
			pTemp0 = cJSON_GetObjectItem(json, "large_image");
			if(pTemp0)
			{
				STRCPY_Ex(Response->Array[i].large_image, pTemp0->valuestring);
			}
			pTemp0 = cJSON_GetObjectItem(json, "small_image");
			if(pTemp0)
			{
				STRCPY_Ex(Response->Array[i].small_image, pTemp0->valuestring);
			}



		}
	}
	cJSON_Delete(jsonall);
	return Response;
}
