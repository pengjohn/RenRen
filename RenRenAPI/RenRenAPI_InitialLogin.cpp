/*!
*@FileName: RenRenAPI_InitialLogin.cpp
*@Author: pengzhixiong@GoZone
*@Date: 2011-1-25
*@Log: 	Author 			Date 		Description
*
*@section Copyright
* =======================================================================<br>
*          Copyright ? 2010-2012 GOZONE <br>
*                All Rights Reserved.<br>
* The file is generated by ReeRen_Component Wizard for Tranzda Mobile Platform <br>
* =======================================================================<br>
*/

#include "RenRenAPICommon.h"

/**
 * JsonÎÄ¼þ½âÎö
 *
 * \param text
 *
 * \return 
 */
void* RenRenAPI_JsonParse_InitialLogin(char *text)
{
	cJSON *json;
	cJSON *pTemp0;
	tResponseInitialLogin*  Response = new tResponseInitialLogin;
	memset(Response, 0 , sizeof(tResponseInitialLogin));

	json = cJSON_Parse(text);

	pTemp0 = cJSON_GetObjectItem(json, "session_key");
	if(pTemp0)
	{
		STRCPY_Ex(Response->session_key, pTemp0->valuestring);
	}
	pTemp0 = cJSON_GetObjectItem(json, "uid");
	if(pTemp0)
	{
		Response->uid = pTemp0->valueint;
	}
	pTemp0 = cJSON_GetObjectItem(json, "error_code");
	if(pTemp0)
	{
		Response->error_code = pTemp0->valueint;
	}
	pTemp0 = cJSON_GetObjectItem(json, "error_msg");
	if(pTemp0)
	{
		STRCPY_Ex(Response->error_msg, pTemp0->valuestring);
	}
	pTemp0 = cJSON_GetObjectItem(json, "ticket");
	if(pTemp0)
	{
		STRCPY_Ex(Response->ticket, pTemp0->valuestring);
	}



	cJSON_Delete(json);
	return Response;
}