/*!
*@FileName: RenRenAPI_RequestsGetFriendRequests.cpp
*@Author: zbingwen@GoZone
*@Date: 2010-12-29
*@Log: 	Author 			Date 		Description
*
*@section Copyright
* =======================================================================<br>
*          Copyright ? 2010-2012 GOZONE <br>
*                All Rights Reserved.<br>
* The file is generated by ReeRen_Component Wizard for Tranzda Mobile Platform <br>
* =======================================================================<br>
*/

#include "RenRenAPICommon.h"

/**
 * JsonÎÄ¼þ½âÎö
 *
 * \param text
 *
 * \return 
 */
void* RenRenAPI_JsonParse_RequestsGetFriendRequests(char *text)
{
	cJSON *json;
	cJSON *pTemp0;
	tResponseRequestsGetFriendRequests*  Response = new tResponseRequestsGetFriendRequests;
	memset(Response, 0 , sizeof(tResponseRequestsGetFriendRequests));

	json = cJSON_Parse(text);

	pTemp0 = cJSON_GetObjectItem(json, "total");
	if(pTemp0)
	{
		Response->total = pTemp0->valueint;
	}
	pTemp0 = cJSON_GetObjectItem(json, "guest");
	if(pTemp0)
	{
		int nSize1 = 0, i = 0;
		nSize1 = cJSON_GetArraySize(pTemp0);
		Response->nSize_guest = nSize1;
		if( nSize1 != 0 )
		{
			Response->guest = NULL;
			Response->guest = (RequestsGetFriendRequests_guest*) malloc(sizeof( RequestsGetFriendRequests_guest ) * nSize1 );
			memset(Response->guest, 0 , sizeof(RequestsGetFriendRequests_guest) * nSize1 );
		}
		for ( i = 0; i < nSize1; i++ )
		{
			cJSON *Item1 = NULL, *pTemp1 = NULL;
			Item1 = cJSON_GetArrayItem(pTemp0,i);
			pTemp1 = cJSON_GetObjectItem(Item1, "uid");
			if(pTemp1)
			{
				Response->guest[i].uid = pTemp1->valueint;
			}
			pTemp1 = cJSON_GetObjectItem(Item1, "headurl");
			if(pTemp1)
			{
				STRCPY_Ex(Response->guest[i].headurl, pTemp1->valuestring);
			}
			pTemp1 = cJSON_GetObjectItem(Item1, "name");
			if(pTemp1)
			{
				STRCPY_Ex(Response->guest[i].name, pTemp1->valuestring);
			}
			pTemp1 = cJSON_GetObjectItem(Item1, "network");
			if(pTemp1)
			{
				STRCPY_Ex(Response->guest[i].network, pTemp1->valuestring);
			}

		}
	}


	cJSON_Delete(json);
	return Response;
}